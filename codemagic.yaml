workflows:
  react-native-expo-android:
    name: TN Happy Kids - Android Release
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      node: latest
      groups:
        - android_signing
        - google_oauth
    scripts:
      - name: Install dependencies
        script: |
          cd my-expo-app
          npm install
          npx expo install expo-build-properties expo-splash-screen
      - name: Expo Prebuild
        script: |
          cd my-expo-app
          npx expo prebuild --platform android
      - name: Setup release signing
        script: |
          echo $CM_KEYSTORE | base64 --decode > /tmp/keystore.jks
      - name: Patch build.gradle for release signing
        script: |
          python3 << 'EOF'
with open("my-expo-app/android/app/build.gradle", "r") as f:
    content = f.read()

# Add release signing config inside signingConfigs block
content = content.replace(
    '''        }
    }
    buildTypes {''',
    '''        }
        release {
            storeFile file(System.getenv("KEYSTORE_PATH") ?: "/tmp/keystore.jks")
            storePassword System.getenv("CM_KEYSTORE_PASSWORD")
            keyAlias System.getenv("CM_KEY_ALIAS")
            keyPassword System.getenv("CM_KEY_PASSWORD")
        }
    }
    buildTypes {'''
)

# Switch release buildType to use release signing config
content = content.replace(
    '''        release {
            // Caution! In production, you need to generate your own keystore file.
            // see https://reactnative.dev/docs/signed-apk-android.
            signingConfig signingConfigs.debug''',
    '''        release {
            signingConfig signingConfigs.release'''
)

with open("my-expo-app/android/app/build.gradle", "w") as f:
    f.write(content)
print("build.gradle patched successfully")
EOF
      - name: Build APK & AAB (signed with release key)
        script: |
          export KEYSTORE_PATH=/tmp/keystore.jks
          export CM_KEYSTORE_PASSWORD=$CM_KEYSTORE_PASSWORD
          export CM_KEY_ALIAS=$CM_KEY_ALIAS
          export CM_KEY_PASSWORD=$CM_KEY_PASSWORD
          cd my-expo-app/android
          ./gradlew assembleRelease
          ./gradlew bundleRelease
      - name: Verify APK fingerprint
        script: |
          apksigner verify --print-certs \
            my-expo-app/android/app/build/outputs/apk/release/app-release.apk
      - name: Verify AAB fingerprint
        script: |
          keytool -printcert -jarfile \
            my-expo-app/android/app/build/outputs/bundle/release/app-release.aab 2>&1 | head -20
    artifacts:
      - my-expo-app/android/app/build/outputs/apk/release/*.apk
      - my-expo-app/android/app/build/outputs/bundle/release/*.aab
    publishing:
      email:
        recipients:
          - dineshwebdev2003@gmail.com
